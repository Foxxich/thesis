\section{Implementacja wybranych naukowych metod}
Celem niniejszego przeglądu jest analiza wybranych implementacji stosowanych w wykrywaniu fake newsów. Podzielono je na pięć głównych kategorii: \textbf{modele oparte na drzewach} (Tree-Based Models), \textbf{modele liniowe i statystyczne} (Linear and Statistical Models), \textbf{sieci neuronowe} (Neural Networks), \textbf{zespoły klasyfikatorów} (Ensemble Classifiers) oraz \textbf{modele zoptymalizowane} (Optimized Models). W każdej z tych kategorii omówiono charakterystyczne cechy oraz innowacyjne modyfikacje w implementacjach, które przyczyniają się do poprawy skuteczności klasyfikacji.
Pragniemy podkreślić, że wszystkie 16 omawianych podejść i implementacji bazują na 16 różnych metodach naukowych, stanowiących fundament analizy. Wybór tych konkretnych metod został podyktowany ich udokumentowaną skutecznością w literaturze naukowej, ze szczególnym uwzględnieniem publikacji z ostatnich lat, które koncentrują się na analizie danych tekstowych i detekcji dezinformacji. Metody te zostały wyselekcjonowane na podstawie ich zdolności do adresowania kluczowych wyzwań w tym obszarze, takich jak przetwarzanie danych, modelowanie złożonych zależności semantycznych oraz radzenie sobie z niezrównoważonymi zbiorami danych, charakterystycznymi dla problematyki fake newsów. Dodatkowo, istotnym czynnikiem wyboru była łatwość implementacji, dostępność propozycji kodu źródłowego w popularnych bibliotekach, gotowych implementacji, oraz programistyczna wygoda, ułatwiająca adaptację i testowanie tych metod w praktyce.

Dodatkowo, proponujemy traktować każde podejście, opisane w jednym z 16 artykułów naukowych, jako odrębną metodę, co ułatwi ich prezentację i wyjaśnienie. Jednocześnie podkreślamy, że autorskie metody, będą omówione w kolejnych rozdziałach, zostaną przedstawione jako metody numer 17, 18, 19 i 20, uzupełniając cel badania. Autorskie metody zostały opracowane na podstawie analizy 16 wspominanych wyżej podejść naukowych, co pozwoliło na stworzenie skutecznych rozwiązań.

\subsection{Modele oparte na drzewach}
\subsubsection{metoda12}
\texttt{metoda12} – Random Forest i CatBoost: dane treningowe i testowe są przyjmowane, sprawdzana jest ich poprawność wymiarów, następnie oba modele są trenowane równolegle. Random Forest jest trenowany na danych treningowych, CatBoost z monitorowaniem danych testowych, a wyniki dla obu modeli są zapisywane w słowniku i zwracane jako struktura zawierająca wytrenowane klasyfikatory Random Forest i CatBoost wraz z danymi testowymi \texttt{X\_test}, czyli tablicą cech testowych, oraz etykietami \texttt{y\_test} w formie tablicy z wartościami 0 i 1 oznaczającymi fałsz lub prawdę, w celu późniejszego wyboru najlepszych w kroku analizy. 

Pomysł na implementację \texttt{metoda12} się bazuje na artykule \cite{metoda12}, który nas zainteresował swoją równoległością oraz podejściem do integracji różnych modeli. Różnica pomiędzy naszą implementacją a podejściem opisanym w artykule polega na tym, że implementacja z artykułu uwzględnia dodatkowe ustawienia dla modelu Random Forest, które nie są obecne w naszej wersji. W artykule określono, jak model powinien oceniać jakość podziału (używając miary o nazwie Gini), minimalną liczbę próbek potrzebną do podzielenia węzła na mniejsze części (ustaloną na dwie próbki), minimalną liczbę próbek wymaganych w liściach drzewa (ustaloną na jedną próbkę) oraz sposób losowego wyboru próbek do trenowania każdego drzewa. Nasza implementacja nie uwzględnia tych szczegółowych ustawień, skupiając się jedynie na ogólnej liczbie drzew. Szczegóły implementacji są pokazane na podanym diagramie \ref{fig:diagram_metoda12}.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.4\textwidth]{img/diagrams/metoda12.drawio.png}
	\caption{Schemat metody 12.}
	\label{fig:diagram_metoda12}
\end{figure}

\subsection{Sieci neuronowe}
\subsubsection{metoda1}
\texttt{metoda1} – Ensemble modeli MLP: trenuje się pięć modeli sieci neuronowych, z których każdy ma losowo wybrane parametry, takie jak liczba neuronów w warstwach, tempo uczenia czy poziom odrzucania (dropout), aby zwiększyć różnorodność. Każdy model jest budowany sekwencyjnie, warstwa po warstwie, z trzema warstwami ukrytymi, które zawierają normalizację danych, funkcję aktywacji LeakyReLU i odrzucanie części neuronów, co zapobiega przeuczeniu. Na końcu jest warstwa wyjściowa z funkcją sigmoidalną, która daje wynik między 0 a 1. Modele są trenowane przez 20 cykli, z partiami po 64 próbki, na danych treningowych, a ich skuteczność jest sprawdzana na danych testowych. Szybkość uczenia jest automatycznie zmniejszana, jeśli wyniki na danych testowych przestają się poprawiać, co pomaga w lepszym dopasowaniu modelu. 

Po wytrenowaniu każdego modelu jego predykcje na danych testowych są zapisywane, a następnie uśredniane, aby uzyskać ostateczny wynik. Wyniki zwracają listę wytrenowanych modeli oraz cechy zbioru walidacyjnego \texttt{X\_test} w formie tablicy danych testowych i etykiety \texttt{y\_test} jako tablicę z wartościami 0 lub 1 oznaczającymi fałsz lub prawdę.

Predykcje są uśredniane dla 5 modeli, obliczając średnią arytmetyczną danych wyjściowych. Różnica pomiędzy naszą implementacją a artykułem polega na tym, że artykuł opisuje sekwencyjne uczenie zespołowe z MLP w dwóch etapach (wielokrotne klasyfikatory binarne i MLP dla decyzji finalnej), podczas gdy kod naszej implementuje równoległe trenowanie wielu modeli MLP z uśrednianiem predykcji bez podziału na etapy.

Nasza implementacja \texttt{metoda1} czerpie inspirację z artykułu \cite{metoda1}. Wspólnym elementem jest wykorzystanie zespołu klasyfikatorów binarnych opartych na sieciach neuronowych do klasyfikacji 0 i 1, a także zastosowanie wielowarstwowego perceptronu (MLP) w procesie decyzyjnym. Obie metody opierają się na danych tekstowych, które są przekształcane w liczby przed klasyfikacją. Różnica polega na tym, że artykuł opisuje sekwencyjne uczenie zespołowe w dwóch etapach – najpierw trenowanie wielokrotnych klasyfikatorów binarnych, a następnie użycie MLP do finalnej decyzji na podstawie ukrytych cech, podczas gdy nasza implementacja trenuje wiele modeli MLP równolegle i łączy ich wyniki przez uśrednianie predykcji, bez podziału na etapy. Szczegóły naszej koncepcji przedstawia diagram \ref{fig:diagram_metoda1}.
\begin{figure}[H]
	\centering
	\includegraphics[width=0.8\textwidth]{img/diagrams/metoda1.drawio.png}
	\caption{Schemat metody 1.}
	\label{fig:diagram_metoda1}
\end{figure}

\subsubsection{metoda4}
\texttt{metoda4} – Bi-LSTM, CNN i sieć MLP: połączenie sieci neuronowych z klasyfikacją sigmoidalną. 

Dane wejściowe są najpierw przekształcane, aby były dwuwymiarowe, potem ujednolicane pod względem wartości, a ich długość jest sprawdzana, by pasowała do wymagań sieci. Następnie tworzona jest warstwa wejściowa, przez którą dane przechodzą do trzech części: dwukierunkowej warstwy LSTM (Bi-LSTM), warstwy analizującej wzorce (CNN) z redukcją wymiarów, oraz prostej warstwy MLP. Wyniki z tych części są spłaszczane, łączone i przetwarzane przez dodatkową warstwę MLP, a na końcu klasyfikowane za pomocą funkcji sigmoidalnej. Model jest trenowany przez 3 cykle z partiami po 256 próbek, z danymi testowymi używanymi do walidacji. 

Wyniki są zwracane jako wytrenowany model oraz przygotowane dane testowe. Wytrenowany model to gotowa sieć neuronowa, która może przewidywać, czy dana wiadomość jest prawdziwa (oznaczona 1) czy fałszywa (oznaczona 0). Przygotowane dane testowe obejmują zmodyfikowaną tablicę cech (\texttt{X\_test\_reshaped}), która jest dostosowana do wymiarów sieci dla zgodności z modelem, oraz tablicę etykiet (\texttt{y\_test}) zawierającą wartości 0 lub 1, służące do sprawdzenia poprawności predykcji.

Pomysł wynika z artykułu \cite{metoda4}, w którym różnica pomiędzy naszą implementacją a oryginałem polega na tym, że artykuł opisuje ensemble oparty na CNN i Bi-LSTM z klasyfikacją wieloklasową za pomocą Softmax, podczas gdy nasza implementacja skupia się na klasyfikacji binarnej, łącząc Bi-LSTM, CNN i MLP w jednej sieci bez oddzielnych etapów analizy cech. Chcąc zaprezentować nasz pomysł, szczegóły implementacji są pokazane na podanym diagramie \ref{fig:diagram_metoda4}.
\begin{figure}[H]
	\centering
	\includegraphics[width=0.4\textwidth]{img/diagrams/metoda4.drawio.png}
	\caption{Schemat metody 4.}
	\label{fig:diagram_metoda4}
\end{figure}

\subsubsection{metoda15}
\texttt{metoda15} – Ensemble modeli Bi-LSTM: modeli zespołowe oparte na dwukierunkowej warstwie LSTM z tokenizacją i wyrównaniem długości sekwencji tekstowych.

Dane wejściowe są najpierw dzielone na pojedyncze słowa (tokenizacja), a ich długości są wyrównywane do wspólnej wartości, aby pasowały do wymagań sieci. Następnie tworzona jest warstwa wejściowa, przez którą dane przechodzą do trzech modeli Bi-LSTM, które analizują tekst w obu kierunkach aby lepiej zrozumieć kontekst. Każdy model jest kompilowany z optymalizatorem Adam, używając funkcji straty \texttt{binary\_crossentropy} do klasyfikacji binarnej. Wyniki z tych modeli są łączone i przetwarzane, a na końcu klasyfikowane za pomocą funkcji sigmoidalnej, która określa, czy wiadomość jest prawdziwa (1) czy fałszywa (0).

Modele są trenowane przez maksymalnie 20 cykli z partiami po 64 próbki, z danymi testowymi używanymi do sprawdzenia postępów. W trakcie trenowania stosowany jest mechanizm wczesnego zatrzymania (EarlyStopping), który monitoruje stratę na danych walidacyjnych i przywraca najlepsze wagi, jeśli wyniki przestają się poprawiać przez 2 cykle, co zapobiega przeuczeniu. Na koniec tworzona jest lista trzech wytrenowanych modeli Bi-LSTM. Wyniki są zwracane jako lista wytrenowanych modeli oraz przygotowane dane testowe.

Lista wytrenowanych modeli to trzy gotowe sieci neuronowe zdolne do przewidywania, czy wiadomość jest prawdziwa (1) czy fałszywa (0). Przygotowane dane testowe obejmują tablicę cech (\texttt{X\_test}), która zawiera przetworzone teksty po tokenizacji i wyrównaniu, oraz tablicę etykiet (\texttt{y\_test}) z wartościami 0 lub 1, służącymi do weryfikacji predykcji.

Dzięki analizie artykułu \cite{metoda15}, powstała implementacja \texttt{metoda15}. Różnica pomiędzy naszą implementacją a oryginałem polega na tym, że artykuł opisuje ensemble Bi-LSTM z VGG-19 i mechanizmem uwagi do klasyfikacji multimodalnej (tekst i obrazy) z niestandardową funkcją straty, podczas gdy nasza implementacja skupia się na klasyfikacji binarnej tekstu za pomocą Bi-LSTM bez dodatkowej warstwy wizualnej. Chcąc zaprezentować nasz pomysł, szczegóły implementacji są pokazane na podanym diagramie \ref{fig:diagram_metoda15}.
\begin{figure}[H]

	\centering
	\includegraphics[width=0.6\textwidth]{img/diagrams/metoda15.drawio.png}
	\caption{Schemat metody 15.}
	\label{fig:diagram_metoda15}
\end{figure}

\subsection{Zespoły klasyfikatorów}
\subsubsection{metoda2}
\texttt{metoda2} – Stacking GradientBoosting i MLP z Logistic Regression: łączenie modeli na podzbiorach cech. To rozwiązanie opiera się na technice stacking, gdzie GradientBoostingClassifier jest trenowany na cechach zachowań społecznych, a MLPClassifier na cechach demograficznych, a następnie predykcje tych modeli są łączone za pomocą Regresji Logistycznej jako meta-modelu.

W przypadku dostarczenia danych wejściowych z co najmniej 6 cechami, zbiór cech jest dzielony na podzbiór demograficzny (pierwsze 5 cech) oraz podzbiór zachowań społecznych (pozostałe cechy). GradientBoostingClassifier z 100 estymatorami, szybkością uczenia 0.1 i maksymalną głębokością 3 jest trenowany na podzbiorze zachowań społecznych, generując prawdopodobieństwa predykcji. Jednocześnie MLPClassifier z jedną warstwą ukrytą o 100 neuronach i maksymalnie 500 iteracjami jest trenowany na podzbiorze demograficznym, również generując prawdopodobieństwa predykcji. Połączone predykcje z obu modeli są następnie używane do treningu Regresji Logistycznej, która integruje wyniki w celu uzyskania ostatecznej klasyfikacji. Dane są przekazywane w formacie DataFrame, a podział na podzbiory jest automatyczny, pod warunkiem wystarczającej liczby cech.

Wyniki są zwracane jako wytrenowany model Regresji Logistycznej, połączone predykcje testowe (\texttt{combined\_preds\_test}) oraz etykiety testowe (\texttt{y\_test}) z wartościami 0 lub 1, służącymi do weryfikacji predykcji. 

Takie podejście w \texttt{metoda2} pozwala na wykorzystanie wszystkich stron obu bazowych modeli, co jest wzorowane artykułem \cite{metoda2}, gdzie różnica polega na zastąpieniu Two-Class Boosted Decision Tree i Two-Class Neural Network odpowiednio GradientBoosting i MLP, przy zachowaniu Regresji Logistycznej jako modelu łączącego. Szczegóły implementacji są pokazane na diagramie \ref{fig:diagram_metoda2}.
\begin{figure}[H]
	\centering
	\includegraphics[width=0.6\textwidth]{img/diagrams/metoda2.drawio.png}
	\caption{Schemat metody 2.}
	\label{fig:diagram_metoda2}
\end{figure}

\subsubsection{metoda3}
\texttt{metoda3} – Dwupoziomowy Voting Classifier: SVM, Naive Bayes, Decision Tree i Bagging/AdaBoost. 

Nasza implementacja posiada trzy klasyfikatory bazowe: SVM \texttt{SVC}, Naive Bayes \texttt{MultinomialNB} oraz Decision Tree \texttt{DecisionTreeClassifier}. Klasyfikatory te są łączone przez \texttt{VotingClassifier} z miękkim głosowaniem, które generuje prawdopodobieństwa predykcji \texttt{predict\_proba} dla \texttt{X\_train} i \texttt{X\_test}, tworząc meta-cechy: \newline\texttt{predictions\_1\_train} i \texttt{predictions\_1\_test}. Te meta-cechy są łączone z oryginalnymi danymi, tworząc rozszerzone zbiory \texttt{X\_train\_meta} i \texttt{X\_test\_meta}, gdzie każda próbka zawiera oryginalne cechy oraz prawdopodobieństwa predykcji z pierwszego poziomu.

Następnie trenowany jest kolejny zespół \texttt{VotingClassifier} \newline\texttt{voting\_clf\_2}, składający się z dwóch klasyfikatorów: \texttt{BaggingClassifier} oraz AdaBoost \texttt{AdaBoostClassifier}. Model ten jest dopasowywany do \texttt{X\_train\_meta} i \texttt{y\_train}, łącząc predykcje z pierwszego poziomu z oryginalnymi cechami, aby uzyskać ostateczną decyzję klasyfikacyjną. Proces ten odzwierciedla ideę zespołowego uczenia z artykułu \cite{metoda3}, gdzie stosowano voting classifier do agregacji wyników różnych modeli, choć w artykule nacisk położono na cechy językowe i word embedding.

Wyniki zwracane przez \texttt{metoda3} obejmują: gotowy model drugiego poziomu \newline(\texttt{voting\_clf\_2}), czyli wytrenowany \texttt{VotingClassifier} z ustawieniami i wagami, meta\-cechy testowe (\texttt{X\_test\_meta}), które łączą oryginalne cechy \texttt{X\_test} z predykcjami pierwszego poziomu, oraz etykiety testowe (\texttt{y\_test}) z wartościami 0 lub 1, umożliwiające ocenę skuteczności modelu.

Główna różnica między nasza implementacją a artykułem dotyczy podejścia do danych i struktury klasyfikacji – artykuł opiera się na ekstrakcji cech językowych i word embedding (TF-IDF, Count Vectorizer) z trzema zestawami LFS, podczas gdy nasza implementacja działa na ogólnych danych, wykorzystując predykcje pierwszego poziomu jako meta-cechy, bez preprocessingu tekstowego. Wspólnym elementem jest użycie voting classifiera i ensemble learningu z modelami takimi jak SVM, Naive Bayes czy Decision Tree, co zwiększa dokładność. Nasza implementacja jest bardziej uniwersalna, ale traci specyficzność cech tekstowych, kluczowych w artykule. Szczegóły implementacji, w tym przepływ danych przez klasyfikatory przedstawia diagram \ref{fig:diagram_metoda3}.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.6\textwidth]{img/diagrams/metoda3.drawio.png}
	\caption{Schemat metody 3.}
	\label{fig:diagram_metoda3}
\end{figure}

\subsubsection{metoda5}
\texttt{metoda5} – Dwuwarstwowy Stacking Classifier z Random Forest, XGBoost i regresją logistyczną: Nasza implementacja stosuje podejście stackingowe do klasyfikacji, zgodne z ideami ensemble learningu opisanymi w artykule \cite{metoda5}, gdzie różne modele maszynowe, takie jak Decision Tree, Random Forest i Extra Tree Classifier, są łączone w celu poprawy dokładności wykrywania fałszywych wiadomości.

Nasza implementacja w pierwszym etapie ma trenowanie dwóch modeli bazowych: Random Forest \texttt{RandomForestClassifier} oraz XGBoost \texttt{XGBClassifier}. Modele te są dopasowywane do \texttt{X\_train} i \texttt{y\_train}, generując prawdopodobieństwa predykcji (\texttt{predict\_proba}) dla klasy pozytywnej, co tworzy nowe cechy dla meta-modelu: \newline\texttt{rf\_preds\_train} i \texttt{xgb\_preds\_train}. Te predykcje są łączone w tablicę danych \newline\texttt{meta\_features\_train} z kolumnami \texttt{rf\_preds} i \texttt{xgb\_preds}, zawierającą dwie cechy na próbkę.

W drugim etapie meta-model, czyli regresja logistyczna (\texttt{LogisticRegression}), jest trenowany na \texttt{meta\_features\_train} i \texttt{y\_train}, ucząc się łączenia predykcji modeli bazowych w celu uzyskania ostatecznej decyzji. Dla danych testowych generowane są predykcje modeli bazowych (\texttt{rf\_preds\_test}, \texttt{xgb\_preds\_test}), które tworzą \texttt{meta\_features\_test}. W artykule \cite{metoda5} podobnie jak w naszej implementacji różne klasyfikatory są łączone poprzez agregację ich wyników, choć w artykule stosowano bagging zamiast stackingu.

Wyniki zwracane przez \texttt{metoda5} obejmują: gotowy meta-model (\texttt{meta\_model}), czyli wytrenowaną regresję logistyczną z ustawieniami i wagami, cechy testowe \newline(\texttt{meta\_features\_test}), które łączą predykcje Random Forest i XGBoost dla \texttt{X\_test} oraz etykiety testowe (\texttt{y\_test}) z wartościami 0 lub 1, umożliwiające ocenę modelu. Struktura ta pozwala na analizę skuteczności modelu, podobnie jak w artykule, gdzie oceniano dokładność na zbiorach ISOT i Liar.

\texttt{metoda5} różni się od podejścia opisanego w artykule \cite{metoda5}. W artykule autorzy skupili się na ekstrakcji 26 cech tekstowych z danych tekstowych, które następnie klasyfikowano za pomocą ensemble opartego na baggingu z Decision Tree, Random Forest i Extra Tree Classifier. Nasza implementacja nie wymaga ekstrakcji specyficznych cech tekstowych, działając na ogólnych danych wejściowych (\texttt{X\_train}, \texttt{X\_test}), i wykorzystuje stacking z Random Forest i XGBoost jako modelami bazowymi oraz regresją logistyczną jako meta-modelem, zamiast baggingu. Wspólną cechą jest zastosowanie ensemble learningu do poprawy dokładności poprzez łączenie różnych klasyfikatorów. Nasza metoda jest bardziej uniwersalna, ponieważ nie zakłada określonego typu danych, ale traci specyficzność cech tekstowych, które w artykule znacząco poprawiły wyniki po ekstrakcji cech. Artykuł podkreśla znaczenie preprocessingu: tokenizacja, usuwanie stop-words, stemming z użyciem NLTK, czego nasza implementacja nie obejmuje, koncentrując się na warstwie modelowania. Szczegóły implementacji przedstawia diagram \ref{fig:diagram_metoda5}.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.6\textwidth]{img/diagrams/metoda5.drawio.png}
	\caption{Schemat metody 5.}
	\label{fig:diagram_metoda5}
\end{figure}

\subsubsection{metoda6}
\texttt{metoda6} – Weighted Convolutional Neural Network Ensemble (WCNNE) z AdaBoost i opcjonalnymi osadzeniami BERT: Nasza implementacja stosuje podejście zespołowe do klasyfikacji sentymentów, zgodne z ideami ensemble learningu opisanymi w artykule \cite{metoda6}, gdzie różne modele, w tym konwolucyjne sieci neuronowe (CNN), są łączone w celu poprawy dokładności analizy sentymentów, szczególnie na danych tekstowych.

Nasza implementacja w \texttt{metoda6} rozpoczyna się od otrzymania danych treningowych (\texttt{X\_train}, \texttt{y\_train}) i testowych (\texttt{X\_test}, \texttt{y\_test}). Dane tekstowe w \texttt{X\_train} i \texttt{X\_test} są przekształcane na osadzenia za pomocą pretrenowanego modelu BERT \newline(\texttt{bert-base-uncased}), co pozwala na lepsze zrozumienie semantyki tekstu, podobnie jak w artykule, gdzie BERT jest wykorzystywany do ekstrakcji cech tekstowych. Osadzenia są generowane z użyciem \texttt{BertTokenizer} i \texttt{TFBertModel}, tworząc trójwymiarowe tablice o wymiarach: liczba próbek, maksymalna długość sekwencji, wymiar osadzenia. Następnie dane są skalowane za pomocą \texttt{StandardScaler}, tworząc \texttt{X\_train\_scaled} i \texttt{X\_test\_scaled}, aby zapewnić normalizację wymaganą przez modele CNN, co odpowiada standardowemu preprocessingowi w artykule. Określa się kształt wejścia \texttt{input\_shape}, \texttt{embedding\_dim} na podstawie wymiarów \texttt{X\_train\_scaled} oraz liczbę klas \newline(\texttt{num\_classes}) na podstawie unikalnych wartości w \texttt{y\_train}, umożliwiając elastyczne dostosowanie modelu do klasyfikacji binarnej lub wieloklasowej.

W kolejnym etapie definiuje się klasyfikator bazowy w postaci konwolucyjnej sieci neuronowej (CNN) zaimplementowanej w metodzie \texttt{create\_cnn\_model}, zgodnej z konfiguracją z artykułu. Model CNN składa się z dwóch warstw \texttt{Conv1D}, przeplatanych warstwami \texttt{MaxPooling1D}, warstwy \texttt{Dropout} dla regularyzacji oraz warstwy \texttt{Flatten} i \texttt{Dense} z aktywacją sigmoidalną dla klasyfikacji binarnej, skompilowanej z optymalizatorem Adam. Klasyfikator CNN jest opakowany w \texttt{KerasClassifier}. Następnie tworzony jest klasyfikator zespołowy WCNNE z użyciem \texttt{AdaBoostClassifier}, który łączy trzy instancje CNN z szybkością uczenia \texttt{learning\_rate=0.01}, co odpowiada strukturze WCNNE opartej na AdaBoost opisanej w \cite{metoda6}. Model \texttt{model} jest trenowany na \texttt{X\_train\_scaled} i \texttt{y\_train}, co odzwierciedla sekwencyjne uczenie słabych klasyfikatorów w AdaBoost dla poprawy wydajności.

Wyniki są zwracane w następujący sposób: gotowy klasyfikator zespołowy (\texttt{model}), zawierający wytrenowane CNN i ustawienia AdaBoost, dane testowe (\texttt{X\_test}), które są osadzeniami oraz etykiety testowe (\texttt{y\_test}) z wartościami 0, 1, umożliwiające ocenę modelu.

\subsubsection{metoda8}
\texttt{metoda8} – Voting Classifier z Random Forest i XGBoost: probabilistyczne głosowanie predykcji.

W artykule \cite{metoda8} omówiono różne strategie ensemble learningu, takie jak bagging, boosting oraz głębokie modele neuronowe, stosowane w celu poprawy generalizacji modeli uczenia maszynowego, w tym Random Forest oparty na baggingu, modele oparte na boostingu, takie jak XGBoost, oraz głębokie sieci neuronowe, takie jak DBN, które zwiększają wydajność dzięki hierarchicznemu uczeniu cech, osiągając lepsze wyniki w klasyfikacji dzięki różnorodności predykcji. 

Nasza implementacja w \texttt{metoda8} całkowicie jest podobna do wspominanej z \cite{metoda8}, realizując podejście zespołowe do klasyfikacji binarnej lub wieloklasowej poprzez połączenie Random Forest, XGBoost oraz głębokiej sieci neuronowej (DBN) w klasyfikatorze Voting Classifier z miękkim głosowaniem, co w pełni odpowiada strategiom ensemble learningu opisanym w artykule, szczególnie w aspekcie włączenia głębokich modeli. Proces rozpoczyna się od przygotowania danych treningowych (\texttt{X\_train}, \texttt{y\_train}) i testowych (\texttt{X\_test}, \texttt{y\_test}). Jeśli \texttt{X\_train} lub \texttt{X\_test} są macierzami rzadkimi, konwertuje się je na macierze gęste, co jest zgodne z potrzebą odpowiedniego formatowania danych w artykule, gdzie różne zestawy danych wymagały preprocessingu. 

Następnie dane są skalowane za pomocą \texttt{StandardScaler}, tworząc \texttt{X\_train\_scaled} i \texttt{X\_test\_scaled}, aby zapewnić normalizację wymaganą przez głębokie modele neuronowe, co odzwierciedla standardowe praktyki preprocessingu dla DBN. Określa się liczbę cech (\texttt{input\_dim}) na podstawie \texttt{X\_train\_scaled.shape[1]} oraz liczbę klas (\texttt{num\_classes}) na podstawie unikalnych wartości w \texttt{y\_train}, co umożliwia dynamiczne dostosowanie modelu DBN do problemu klasyfikacji. Definiuje się trzy klasyfikatory bazowe: \texttt{RandomForestClassifier} z 100 estymatorami oznaczany jako \texttt{rf}, oparty na strategii baggingu; \texttt{XGBClassifier} z 100 estymatorami, oznaczany jako \texttt{xgb}, oparty na boostingu; oraz \texttt{KerasClassifier} opakowujący model DBN zdefiniowany w \texttt{create\_dbn\_model}, oznaczany jako \texttt{dbn}, z warstwami z aktywacją ReLU, warstwami Dropout dla regularyzacji oraz warstwą wyjściową z aktywacją sigmoidalną dla klasyfikacji binarnej lub softmax dla wieloklasowej. Wybór tych klasyfikatorów dokładnie pokrywa się z omówieniem baggingu, boostingu i głębokich modeli w artykule jako kluczowych technik ensemble learningu. 

Klasyfikatory są łączone w \texttt{VotingClassifier}, oznaczany jako \texttt{ensemble\_model}, z miękkim głosowaniem, które uśrednia prawdopodobieństwa predykcji z \texttt{rf}, \texttt{xgb} i \texttt{dbn}, podobnie jak w artykule strategie ensemble łączyły predykcje dla poprawy wydajności. Model \texttt{ensemble\_model} jest trenowany na \texttt{X\_train\_scaled} i \texttt{y\_train}, co odpowiada etapowi uczenia w artykule, gdzie modele ensemble, w tym głębokie, były trenowane na różnych zbiorach danych po odpowiednim preprocessingu. Po wytrenowaniu metoda zwraca \texttt{ensemble\_model}, \texttt{X\_test} oraz \texttt{y\_test}, gdzie \texttt{y\_test} zawiera etykiety binarne (0 lub 1) lub wieloklasowe, umożliwiając dalszą ocenę modelu, co jest zgodne z praktyką oceny modeli w \cite{metoda8}.

\subsubsection{metoda9}
\texttt{metoda9} – Voting Classifier z Logistic Regression, Random Forest i SVC oraz porównanie modeli zespołowych.

W artykule \cite{metoda9} opisano podejście oparte na ensemble learning do wykrywania fałszywych wiadomości, stosując różne techniki zespołowe, w tym Voting Classifier z \newline \texttt{LogisticRegression}, \texttt{RandomForestClassifier} oraz innymi modelami, takimi jak KNN lub LSVM, a także Bagging i Boosting, w tym AdaBoost i XGBoost, oceniane za pomocą metryk takich jak dokładność, precyzja, recall i F1-score na czterech zbiorach danych, osiągając średnią dokładność 92.25\% dla metod zespołowych. 

Nasza implementacja w \texttt{metoda9} zachowuje te kluczowe cechy, opierając się na podobnym podejściu ensemble learningu do klasyfikacji binarnej fałszywych wiadomości, z wykorzystaniem kombinacji modeli i ich predykcji dla poprawy dokładności. W artykule dane są przygotowywane z użyciem narzędzia LIWC do ekstrakcji cech językowych, co w naszym procesie odpowiada treningowi na danych \texttt{X\_train} (cechy treningowe) i \texttt{y\_train} (etykiety treningowe), które są już w odpowiedniej formie wektorowej, umożliwiając bezpośrednie zastosowanie modeli. Proces rozpoczyna się od zdefiniowania modeli bazowych: \texttt{LogisticRegression} oznaczanego jako \texttt{log\_reg}, \texttt{RandomForestClassifier} oznaczanego jako \texttt{rf} oraz \texttt{KNeighborsClassifier} oznaczanego jako \texttt{knn}, które pokrywają się z wyborem modeli jak w artykule, tworząc podstawę dla uczenia zespołowego. 

Dodatkowo stosujemy \texttt{BaggingClassifier} z \texttt{rf} jako estymatorem, oznaczany jako \texttt{bagging}, oraz \texttt{AdaBoostClassifier}, oznaczany jako \texttt{boosting}, co jest zgodne z technikami Bagging i Boosting (AdaBoost) opisanymi w \cite{metoda9}, wzmacniając ideę ensemble learningu. Modele te są łączone w \texttt{VotingClassifier}, oznaczanym jako \texttt{voting}, co pozwala na integrację predykcji prawdopodobieństw z każdego modelu, podobnie jak w artykule Voting Classifier łączył wyniki modeli dla uzyskania wysokiej dokładności. Trening wszystkich modeli, w tym \texttt{rf}, \texttt{bagging}, \texttt{boosting} i \texttt{voting}, odbywa się na \texttt{X\_train} i \texttt{y\_train}, co odpowiada etapowi uczenia w artykule, gdzie różne algorytmy były trenowane na danych z cechami językowymi. 

Po wytrenowaniu każdy model generuje predykcje \texttt{y\_pred} na \texttt{X\_test} (cechy testowe), a wyniki są oceniane za pomocą \texttt{accuracy\_score}, \texttt{roc\_auc\_score} (jeśli model wspiera predykcje prawdopodobieństw) oraz \texttt{classification\_report} na podstawie \texttt{y\_test} (etykiety testowe z wartościami 0 lub 1), co jest wspólnym elementem z artykułem, gdzie stosowano metryki takie jak dokładność, precyzja, recall i F1-score do oceny wydajności. Metoda zwraca \texttt{model} (najlepszy model), \texttt{X\_test} oraz \texttt{y\_test}, umożliwiając dalszą analizę, podobnie jak w artykule wyniki były dostępne do porównania na różnych zbiorach danych.

\subsubsection{metoda10}
\texttt{metoda10} – Voting Classifier z MLP, Logistic Regression i XGBoost z miękkim głosowaniem.

W artykule \cite{metoda10} jest zaproponowany Voting Classifier z trzema najlepszymi klasyfikatorami wybranymi po walidacji krzyżowej: \texttt{MLPClassifier}, \texttt{LogisticRegression} i \texttt{XGBClassifier}. Modele te są łączone z miękkim głosowaniem (\texttt{voting='soft'}) na podstawie prawdopodobieństw, osiągając dokładność 94.47\% na danych tekstowych po preprocessingu.

Nasza implementacja w \texttt{metoda10} zachowuje kluczowe wspólne cechy tego podejścia, wykorzystując identyczne modele i strukturę ensemble do klasyfikacji binarnej na podstawie danych tekstowych. W artykule modele są trenowane na danych po preprocessingu, co znajduje swoje odzwierciedlenie w naszym procesie, gdzie dane wejściowe \texttt{X\_train} (cechy treningowe) i \texttt{y\_train} (etykiety treningowe) są bezpośrednio używane do treningu, choć w naszym kodzie pomijamy preprocessing, korzystając z wspólnego procesu przygotowania danych dla wszystkich metod, co czyni podejście bardziej uniwersalnym, ale zachowuje zgodność z ideą klasyfikacji na przygotowanych danych. 

Proces rozpoczyna się od inicjalizacji tych samych klasyfikatorów co w artykule: \texttt{MLPClassifier} oznaczanego jako \texttt{mlp}, \texttt{LogisticRegression} oznaczanego jako \newline\texttt{log\_reg} oraz \texttt{XGBClassifier} oznaczanego jako \texttt{xgb}, które są zgodne z wyborem najlepszych modeli w \cite{metoda10}, tworząc podstawę dla uczenia zespołowego. Te modele są następnie łączone w \texttt{VotingClassifier}, oznaczanym jako \texttt{voting\_clf}, z miękkim głosowaniem (\texttt{voting='soft'}), co pozwala na integrację predykcji prawdopodobieństw z każdego modelu, dokładnie tak jak w artykule Voting Classifier opierał się na kombinacji wyników dla osiągnięcia wysokiej dokładności. 

Trening \texttt{voting\_clf} odbywa się na \texttt{X\_train} i \texttt{y\_train}, co odpowiada etapowi uczenia w artykule, gdzie modele były trenowane na danych tekstowych po preprocessingu, a w naszym przypadku dane są już w odpowiedniej formie dzięki wspólnemu pipeline. Po wytrenowaniu \texttt{voting\_clf} jest gotowy do predykcji na \texttt{X\_test} (macierz cech testowych), a metoda zwraca \texttt{voting\_clf}, który zawiera ustawienia i wyniki uczenia, wraz z \texttt{X\_test} oraz \texttt{y\_test} (wektor etykiet testowych z wartościami 0 lub 1), co umożliwia ocenę jakości modelu, podobnie jak w artykule wyniki klasyfikacji służyły do weryfikacji skuteczności, osiągając wspomnianą dokładność. 

Mamy dokładnie to samo podejście co w \cite{metoda10}, opierając się na Voting Classifier z identycznymi modelami \texttt{MLPClassifier}, \texttt{LogisticRegression} i \texttt{XGBClassifier} oraz \texttt{soft} głosowaniem, z tą różnicą, że pomijamy preprocessing w kodzie na rzecz uniwersalnego przygotowania danych, co nie zmienia istoty ensemble methods opisanych w artykule.

\subsubsection{metoda11}
\texttt{metoda11} – Voting Classifier z Random Forest, Logistic Regression i AdaBoost po selekcji cech.

W artykule \cite{metoda11} opisano zastosowanie ensemble methods, w tym Voting Classifier z \texttt{RandomForestClassifier}, \texttt{LogisticRegression} i \texttt{AdaBoostClassifier}, do wykrywania fałszywych wiadomości na podstawie cech tekstowych, osiągając wysoką dokładność dzięki kombinacji predykcji. 

Nasza implementacja \texttt{metoda11} całkiem reprezentuje to podejście, opierając się na identycznym zestawie modeli i idei łączenia ich predykcji w celu poprawy klasyfikacji binarnej fałszywych wiadomości. W artykule wykorzystano cechy tekstowe do treningu modeli, co znajduje swoje odzwierciedlenie w naszym procesie, gdzie dane wejściowe \texttt{X\_train} (cechy treningowe) i \texttt{X\_test} (cechy testowe) są poddawane normalizacji za pomocą \texttt{MinMaxScaler}, tworząc \texttt{X\_train\_scaled} i \texttt{X\_test\_scaled}, aby zapewnić nieujemne wartości wymagane do dalszej selekcji cech, co jest wspólnym krokiem przygotowania danych do klasyfikacji. 

Następnie stosujemy selekcję cech za pomocą \texttt{SelectKBest} z testem Chi-kwadrat, przekształcając \newline \texttt{X\_train\_scaled} w \texttt{X\_train\_selected} oraz \texttt{X\_test\_scaled} w \texttt{X\_test\_selected}, co pozwala skoncentrować się na najważniejszych cechach, podobnie jak w artykule dane tekstowe były optymalizowane przed klasyfikacją. Proces kontynuowany jest przez zdefiniowanie tych samych modeli bazowych co w artykule: \texttt{RandomForestClassifier} oznaczanego jako \texttt{rf}, \texttt{LogisticRegression} oznaczanego jako \texttt{lr} oraz \texttt{AdaBoostClassifier} oznaczanego jako \texttt{adb}, które są zgodne z wyborem modeli w \cite{metoda11}, tworząc podstawę dla uczenia zespołowego. 

Te modele są następnie łączone w \texttt{VotingClassifier}, oznaczanym jako \texttt{voting\_clf}, z miękkim głosowaniem (\texttt{voting='soft'}), co pozwala na integrację predykcji prawdopodobieństw z każdego modelu, dokładnie tak jak w artykule ensemble methods opierały się na kombinacji wyników dla zwiększenia dokładności. Trening \texttt{voting\_clf} odbywa się na \texttt{X\_train\_selected} i \texttt{y\_train} (etykiety treningowe), co odpowiada etapowi uczenia w artykule, gdzie modele były trenowane na przygotowanych danych tekstowych. 

Po wytrenowaniu \texttt{voting\_clf} jest gotowy do predykcji na \texttt{X\_test\_selected}, a metoda zwraca \texttt{voting\_clf}, który zawiera ustawienia i wyniki uczenia, wraz z przetworzonymi danymi testowymi \texttt{X\_test\_selected} w formie macierzy oraz \texttt{y\_test} (etykiety testowe jako wektor z wartościami 0 i 1), co umożliwia ocenę jakości modelu, podobnie jak w artykule wyniki klasyfikacji służyły do weryfikacji skuteczności. Mamy dokładnie to samo podejście co w \cite{metoda11}, opierając się na Voting Classifier z tymi samymi modelami bazowymi i miękkim głosowaniem, z dodaną normalizacją i selekcją cech jako częścią naszego pipeline, co wzmacnia zgodność z ideą ensemble methods opisaną w artykule.

\subsubsection{metoda13 TODO}
\texttt{metoda13} – Stacking z SMOTETomek, wykorzystujący Random Forest, Gradient Boosting, XGBoost i Logistic Regression jako meta-model.

W artykule \cite{metoda13} opisano metody augmentacji danych, jak SMOTE, oraz uczenie zespołowe, w tym stacking z modelami takimi jak \newline \texttt{RandomForestClassifier}, \texttt{GradientBoostingClassifier} i \texttt{XGBClassifier}, do rozwiązywania problemów niezrównoważonych klas, gdzie proces równoważy dane treningowe i łączy predykcje modeli bazowych dla lepszej dokładności. 

Nasza implementacja \texttt{metoda13} zachowuje te kluczowe cechy, opierając się na podobnym podejściu do balansowania danych i uczenia zespołowego w celu poprawy klasyfikacji binarnej. W artykule dane treningowe są równoważone za pomocą technik augmentacji takich jak SMOTE, co znajduje odzwierciedlenie w naszym procesie, gdzie stosujemy \texttt{SMOTETomek} do balansowania \texttt{X\_train} (macierz cech treningowych) i \texttt{y\_train} (wektor etykiet treningowych), tworząc \texttt{X\_train\_resampled} i \texttt{y\_train\_resampled}, które reprezentują zbalansowane dane gotowe do dalszego treningu. Proces rozpoczyna się od tego kroku augmentacji, co jest wspólnym elementem z artykułem, przygotowując dane do uczenia zespołowego. 

Następnie definiujemy modele bazowe: \texttt{RandomForestClassifier} oznaczany jako \texttt{rf}, \texttt{GradientBoostingClassifier} oznaczany jako \texttt{gb} oraz \texttt{XGBClassifier} oznaczany jako \texttt{xgb}, które pokrywają się z modelami opisanymi w artykule, tworząc zestaw \texttt{base\_learners} wykorzystywany w stackingu, co odzwierciedla ideę łączenia predykcji różnych klasyfikatorów dla zwiększenia dokładności. Te modele bazowe są integrowane w ramach \texttt{StackingClassifier}, oznaczanego jako \texttt{stacking\_model}, z meta-modelem \texttt{LogisticRegression}, oznaczanym jako \texttt{meta\_learner}, który wykorzystuje predykcje modeli bazowych do finalnej klasyfikacji, co jest zgodne z koncepcją uczenia zespołowego z artykułu, gdzie predykcje są łączone dla lepszych wyników. 

Trening \texttt{stacking\_model} odbywa się na \texttt{X\_train\_resampled} i \texttt{y\_train\_resampled} z użyciem 5-krotnej walidacji krzyżowej, co pozwala na generowanie meta-cech i uczenie meta-modelu, podobnie jak w artykule stosowano podejście zespołowe do integracji predykcji, choć tam nie określono konkretnie stackingu z walidacją krzyżową. Po wytrenowaniu \texttt{stacking\_model} generuje predykcje \texttt{y\_pred} na \texttt{X\_test} (macierz cech testowych), a wyniki są oceniane za pomocą \texttt{classification\_report} na podstawie \texttt{y\_test} (wektor etykiet testowych z wartościami 0 lub 1), co odpowiada celowi artykułu, czyli poprawie dokładności w problemach niezrównoważonych klas i weryfikacji skuteczności modelu. Metoda zwraca \texttt{stacking\_model}, \texttt{X\_test} oraz \texttt{y\_test}, umożliwiając dalszą analizę, co jest wspólnym elementem z artykułem, gdzie wyniki klasyfikacji również służyły do oceny. 

Główna różnica dotyczy konkretnej konfiguracji stackingu – w artykule nie opisano wprost \texttt{StackingClassifier} z walidacją krzyżową, co jest naszą specyficzną implementacją, ale istota balansowania danych i uczenia zespołowego pozostaje taka sama. Szczegóły implementacji, w tym przepływ danych od \texttt{X\_train} do \texttt{stacking\_model}, zilustrowano na diagramie \ref{fig:diagram_metoda13}.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.6\textwidth]{img/diagrams/metoda13.drawio.png}
	\caption{Schemat metody 13.}
	\label{fig:diagram_metoda13}
\end{figure}

\subsubsection{metoda14}
\texttt{metoda14} – Ensemble learning w formie stackingu, wykorzystujący Random Forest, AdaBoost, SVC oraz Logistic Regression jako meta-model.

W artykule \cite{metoda14} opisano tradycyjne modele machine learning, w tym SVM na przykładzie SVC i Logistic Regression, oraz ensemble learning na przykładzie AdaBoost do wykrywania fake newsów. Dane są wstępnie przetwarzane przez NLTK, stemming, TF-IDF i klasyfikowane na zróżnicowanych zbiorach danych. 

Nasza implementacja \texttt{metoda14} zachowuje wspólne cechy tego podejścia, opierając się na wykorzystaniu tradycyjnych modeli machine learning i ensemble learningu do klasyfikacji binarnej fake newsów. W artykule dane tekstowe są przygotowywane za pomocą NLTK i TF-IDF, podczas gdy u nas preprocessing opiera się na funkcji \texttt{preprocess\_text}, która stosuje stemming z \texttt{SnowballStemmer}, inspirując się podejściem z \cite{snowballstemmer}, gdzie autorzy użyli tego narzędzia do redukcji słów do ich rdzeni, co upraszcza dane wejściowe, takie jak \texttt{X\_train} (cechy treningowe) i \texttt{X\_test} (cechy testowe), w porównaniu do bardziej złożonego TF-IDF z artykułu. 

Proces w naszej implementacji zaczyna się od przygotowania danych, gdzie tekst w \texttt{X\_train} i \texttt{X\_test} jest oczyszczany i stemmowany, co odpowiada etapowi preprocessingu w artykule, choć u nas wynik jest już w formie wektorowej gotowej do klasyfikacji. Następnie inicjalizujemy modele bazowe: \texttt{RandomForestClassifier} oznaczany jako \texttt{rf\_model}, \texttt{AdaBoostClassifier} oznaczany jako \texttt{ab\_model} oraz \texttt{SVC} z liniowym jądrem oznaczany jako \texttt{svc\_model}, co częściowo pokrywa się z artykułem, który wykorzystuje SVM (SVC) i AdaBoost, choć Random Forest jest naszym dodatkiem, zwiększającym różnorodność predykcji. 

Te modele bazowe są łączone w klasyfikatorze \texttt{StackingClassifier}, oznaczanym jako \texttt{stack\_model}, z meta-modelem \texttt{LogisticRegression}, oznaczanym jako \texttt{lr\_model}, co odzwierciedla użycie regresji logistycznej w artykule, ale w ramach stackingu zamiast pojedynczego modelu. Podczas treningu \texttt{stack\_model} uczy się na \texttt{X\_train} i \texttt{y\_train} (etykiety treningowe), gdzie modele bazowe generują predykcje wykorzystywane przez \texttt{lr\_model} do finalnej klasyfikacji, co jest wspólnym elementem ensemble learningu z artykułem, choć tam nie stosowano stackingu, a modele działały bardziej niezależnie. Po wytrenowaniu \texttt{stack\_model} jest gotowy do predykcji na \texttt{X\_test}, a metoda zwraca \texttt{stack\_model}, \texttt{X\_test} oraz \texttt{y\_test} (etykiety testowe w formie wektora binarnego 0 lub 1), co umożliwia ocenę skuteczności, podobnie jak w artykule wyniki były klasyfikowane i oceniane na zróżnicowanych zbiorach danych. Główna różnica dotyczy struktury ensemble – artykuł nie opisuje stackingu ani Random Forest jako modelu bazowego, które wprowadzamy, by zwiększyć elastyczność, podczas gdy preprocessing z \texttt{SnowballStemmer} upraszcza podejście w porównaniu do TF-IDF. Szczegóły implementacji, w tym przepływ danych od \texttt{X\_train} do \texttt{stack\_model}, są pokazane na diagramie \ref{fig:diagram_metoda14}.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.5\textwidth]{img/diagrams/metoda14.drawio.png}
	\caption{Schemat metody 14.}
	\label{fig:diagram_metoda14}
\end{figure}

\subsubsection{Metoda16}
\texttt{Metoda16} – Stacking z meta-cechami: Random Forest, SVC z Logistic Regression. Metoda ta wykorzystuje podejście stackingu, w którym modele bazowe generują meta-cechy, a meta-klasyfikator na ich podstawie dokonuje ostatecznej predykcji.

W oryginalnym podejściu opisanym w artykule \cite{metoda16} proces klasyfikacji przebiega w kilku etapach. Najpierw dane tekstowe są przetwarzane za pomocą biblioteki NLTK, co obejmuje usuwanie stop-słów, stemming i lematyzację, a następnie konwertowane na cechy numeryczne za pomocą metod takich jak TF-IDF i CountVectorizer. Następnie stosuje się ensemble learning w formie stackingu, gdzie modele bazowe, takie jak Random Forest i SVM, generują predykcje prawdopodobieństw klas dla danych treningowych za pomocą walidacji krzyżowej. Te predykcje są używane jako meta-cechy do treningu meta-klasyfikatora, którym jest regresja logistyczna. Modele bazowe są następnie trenowane na pełnym zbiorze treningowym, a meta-cechy dla danych testowych są generowane na podstawie ich predykcji. Ostatecznie meta-klasyfikator dokonuje predykcji na meta-cechach testowych, a wyniki są oceniane za pomocą metryk takich jak dokładność i raport klasyfikacji. 

Nasza implementacja \texttt{metoda16} zachowuje kluczowe wspólne cechy tego podejścia, opierając się na podobnej strukturze stackingu i wykorzystaniu ensemble learningu. W naszej wersji dane wejściowe, czyli \newline\texttt{X\_train} reprezentujące cechy treningowe oraz \texttt{X\_test} odpowiadające cechom testowym, są już przygotowane jako osadzenia wektorowe w ramach wspólnego pipeline, co różni się od preprocessingu NLTK i metod TF-IDF/CountVectorizer stosowanych w artykule, ale zachowuje ideę przekształcenia danych do postaci numerycznej przed klasyfikacją. Proces zaczyna się od inicjalizacji modeli bazowych: \texttt{RandomForestClassifier} oznaczanego jako \texttt{rf\_model} oraz \texttt{SVC} z liniowym jądrem oznaczanego jako \texttt{svc\_model}, co odpowiada użyciu Random Forest i SVM w artykule. 

Następnie generujemy meta-cechy dla danych treningowych, czyli \newline\texttt{meta\_features\_train}, poprzez 5-krotną walidację krzyżową z rozwarstwieniem \newline(\texttt{StratifiedKFold}), gdzie \texttt{rf\_preds\_train} i \texttt{svc\_preds\_train} to predykcje prawdopodobieństw odpowiednio od \texttt{rf\_model} i \texttt{svc\_model} na \texttt{X\_train} i \texttt{y\_train} (etykiety treningowe), łączone w macierz \texttt{meta\_features\_train}, co jest zgodne z podejściem artykułu, w którym predykcje z walidacji krzyżowej stają się meta-cechami. Te meta-cechy są używane do treningu meta-klasyfikatora \texttt{LogisticRegression}, oznaczonego jako \texttt{meta\_model}, na \texttt{meta\_features\_train} i \texttt{y\_train}, co odzwierciedla etap treningu regresji logistycznej w artykule. 

Po tym kroku modele bazowe \texttt{rf\_model} i \texttt{svc\_model} są trenowane na pełnym zestawie \texttt{X\_train} i \texttt{y\_train}, podobnie jak w artykule, gdzie następuje trening na całym zbiorze treningowym. Dla danych testowych generujemy meta-cechy \texttt{meta\_features\_test}, obliczając \texttt{rf\_preds\_test} i \texttt{svc\_preds\_test} jako predykcje prawdopodobieństw od \texttt{rf\_model} i \texttt{svc\_model} na \texttt{X\_test}, a następnie łącząc je w macierz \texttt{meta\_features\_test}, co odpowiada generowaniu meta-cech testowych w artykule. Ostatecznie \texttt{meta\_model} dokonuje predykcji \texttt{y\_pred} na \texttt{meta\_features\_test}, a wyniki są oceniane za pomocą \texttt{classification\_report} i \texttt{accuracy\_score} na podstawie \texttt{y\_test} (etykiety testowe), co jest wspólnym elementem z artykułem, gdzie również stosowano metryki takie jak dokładność i raport klasyfikacji. Metoda zwraca \texttt{meta\_model}, \texttt{meta\_features\_test} oraz \texttt{y\_test}, co umożliwia dalszą analizę.

\subsection{Modele zoptymalizowane}
\subsubsection{Metoda7}
\texttt{Metoda7} – Random Forest z Monte Carlo Dropout i heurystyczną obróbką \newline post-procesową. Metoda7 opiera się na Random Forest jako bazowym klasyfikatorze. 

W oryginalnym podejściu opisanym w artykule \cite{metoda7} proces klasyfikacji przebiega w kilku etapach. Najpierw dane tekstowe są przetwarzane za pomocą pre-trenowanego modelu językowego NewsBERT, który generuje wektory predykcji jako dodatkowe cechy. Następnie stosuje się ensemble pre-trenowanych modeli transformerowych, takich jak DeBERTa, ELECTRA i ERNIE 2.0, z przybliżeniem Bayesowskim, co pozwala na oszacowanie niepewności predykcji poprzez wielokrotne próbkowanie z użyciem techniki dropout. Te modele wspólnie generują predykcje, które są następnie łączone. W końcowym etapie wykorzystywany jest moduł heurystycznej obróbki post-procesowej (SFTN), który koryguje predykcje na podstawie statystycznych meta-atrybutów, takich jak źródło wiadomości, poprawiając dokładność klasyfikacji binarnej (real/fake) na zbiorze FakeNewsNet. 

Nasza implementacja w \texttt{metoda7} różni się od tego podejścia, ale zachowuje pewne wspólne elementy. Zamiast ensemble transformerów i NewsBERT, wykorzystujemy dwa modele: Random Forest, oznaczony jako \texttt{rf\_model}, oraz XGBoost, oznaczony jako \texttt{xgb\_model}, co wynika z faktu, że dane wejściowe, czyli \texttt{X\_train} reprezentujące cechy treningowe oraz \texttt{X\_test} odpowiadające cechom testowym, są już przekształcone w osadzenia wektorowe, eliminując potrzebę tokenizacji czy generowania cech przez modele językowe. 

Proces zaczyna się od przygotowania danych, gdzie \texttt{X\_train} i \texttt{y\_train} (etykiety treningowe) są konwertowane na \texttt{train\_embeddings} i \texttt{train\_labels}, a \texttt{X\_test} oraz \texttt{y\_test} na \texttt{test\_embeddings} i \texttt{test\_labels}, dane zostałe standaryzowane przed dalszym przetwarzaniem. Następnie oba modele, \texttt{rf\_model} i \texttt{xgb\_model}, są trenowane na \texttt{train\_embeddings} i \texttt{train\_labels}, co odpowiada etapowi uczenia w artykule, choć bez użycia transformerów. Random Forest generuje predykcje na \texttt{test\_embeddings} za pomocą symulacji Monte Carlo Dropout, tworząc \texttt{rf\_mean\_preds} (średnie prawdopodobieństwa) i \texttt{rf\_uncertainty} (niepewność) poprzez próbkowanie 80\% drzew w każdej iteracji, z których wyciągane są etykiety \texttt{rf\_pred\_labels}, co określa oszacowanie niepewności w artykule, ale w innej formie. 

XGBoost z kolei oblicza predykcje na \texttt{test\_embeddings} standardowo, dając \newline \texttt{xgb\_mean\_preds} (prawdopodobieństwa) i \texttt{xgb\_pred\_labels} (etykiety), co jest prostszą alternatywą dla ensemble transformerów. Wprowadzamy meta-atrybuty w \texttt{df} jako kolumnę "source", domyślnie ustawioną na "unknown", które trafiają do \texttt{attribute\_probs} i są używane w heurystycznej obróbce post-procesowej, korygując \texttt{rf\_pred\_labels} na \texttt{rf\_final\_preds} oraz \texttt{xgb\_pred\_labels} na \texttt{xgb\_final\_preds}, podobnie jak SFTN w artykule, choć u nas wpływ meta-atrybutów jest ograniczony z powodu ich uproszczenia w porównaniu do różnorodnych źródeł w oryginalnym podejściu. 

Wyniki obu modeli są oceniane na \texttt{test\_labels}, a funkcja zwraca \texttt{rf\_model}, \newline\texttt{xgb\_model}, \texttt{X\_test} i \texttt{y\_test}, co pozwala na dalszą analizę, podczas gdy artykuł skupiał się na zwracaniu skonsolidowanych predykcji z ensemble. Szczegóły implementacji pokazano na diagramie \ref{fig:diagram_metoda7}.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.5\textwidth]{img/diagrams/metoda7.drawio.png}
	\caption{Schemat metody 7.}
	\label{fig:diagram_metoda7}
\end{figure}